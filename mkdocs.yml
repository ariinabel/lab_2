site_name: Calculator Docs
nav:
  - Home: index.md
  - API Reference: api-reference.md
theme: readthedocs
plugins:
  - mkdocstrings:
      handlers:
        python:
          options:
            filters: ["!^_"]
